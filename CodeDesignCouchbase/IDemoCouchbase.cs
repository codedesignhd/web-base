using System;
using System.Collections.Generic;
using System.Text;
using Couchbase;

namespace CodeDesign.Couchbase
{
    public interface IDemoCouchbase
    {
        //T Get<T>(string key) where T : class;
        //List<T> MGet<T>(IEnumerable<string> keys) where T : class;

        //bool Update<T>(string key, object data) where T : class;

        //bool SetExpire(string key, TimeSpan expire);

        //bool SetExpire(string key, double seconds);

        //bool Set(string key, object data, TimeSpan expire);
        //bool Set(string key, object data, double seconds);
    }
}
